# configure, build and running the application:
# install dependencies:
> npm install

# build the application:
> npm run build

# test the application:
> npm run test

# play the game:
> npm run start
